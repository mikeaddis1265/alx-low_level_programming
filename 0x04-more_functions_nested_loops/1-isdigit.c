#include"main.h"
/**
 * _isdigit-checks acharacter if it is a digit or not
 * @c -int data type
 * Return -1 if it is 0 otherwise
*/
int _isdigit(int c)
{
    if(c >= 48 && c <=57)  
    {
        return  (1);
    }
    else
    {
        return  (0);
    }
}
